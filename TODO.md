check for UEFI boot mode: ls /sys/firmware/efi/efivars
  - exit, not supported
keyboard layout configurator
allow manual partition, choose partition for install, rather than forcing automation
Time Zone
  /usr/share/zoneinfo
    Select Region: list dirs
